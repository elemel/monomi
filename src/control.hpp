#ifndef MONOMI_CONTROL_HPP
#define MONOMI_CONTROL_HPP

namespace monomi {
    enum Control {
        actionControl,
        downControl,
        jumpControl,
        leftControl,
        rightControl,
        upControl,

        controlCount
    };
}

#endif // MONOMI_CONTROL_HPP
